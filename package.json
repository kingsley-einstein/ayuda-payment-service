{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p ./",
    "clean:local": "if exist ./build rmdir /s/q build",
    "clean:unix": "if -d ./build; then rm -rf build; fi",
    "build:local": "npm run clean:local && npm run build",
    "build:unix": "npm run clean:unix && npm run build",
    "start:local": "npm run build:local && set NODE_ENV=development&& node build/app",
    "start:production": "export NODE_ENV=production && export PORT=80 && export NODE_TLS_REJECT_UNAUTHORIZED=0 && node build/app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/cloud-config-client": "^1.4.0",
    "@types/dotenv": "^8.2.0",
    "@types/eureka-js-client": "^4.5.2",
    "@types/express": "^4.17.6",
    "@types/mongoose": "^5.7.21",
    "@types/paystack": "^2.0.0",
    "@types/request": "^2.48.5",
    "@types/request-promise": "^4.1.46",
    "cloud-config-client": "^1.5.2",
    "dotenv": "^8.2.0",
    "eureka-js-client": "^4.5.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.15",
    "paystack": "^2.0.1",
    "paystack-transfer": "^0.1.2",
    "request": "^2.88.2",
    "request-promise": "^4.2.5",
    "typescript": "^3.7.2"
  }
}
